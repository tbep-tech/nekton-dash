---
title: "TAMPA BAY NEKTON INDEX DASHBOARD"
output: 
  flexdashboard::flex_dashboard:
     logo: www/tarponlogo.png
     social: menu
     source_code: "https://github.com/tbep-tech/nekton-dash"
runtime: shiny
css: styles.css
---

```{r setup, include=F}
knitr::opts_chunk$set(echo = F, message = F, warning = F)

library(flexdashboard)
library(tidyverse)
library(tbeptools)
# devtools::load_all('../tbeptools')
library(mapedit)
library(mapview)
library(leaflet.extras)
library(sf)
library(reactable)
library(shinydashboard)
library(plotly)
library(shinyWidgets)
library(extrafont)

# # style file
# styles <- readLines('https://raw.githubusercontent.com/tbep-tech/css-styling/master/styles.css')
# writeLines(styles, 'styles.css')

source('R/funcs.R')

data(tbniscr)

fml <- 'Lato Light'
maxyr <- 2019
perc <- c(32, 46)
cols <- c('red', 'yellow', 'green')

# base matrix
tbnimat <- show_tbnimatrix(tbniscr, family = fml, txtsz = NULL) 

# fimstations filtered by bay segments
fimstations <- fimstations[tbseg, ]

# annually averaged data
tbniyrs <- tbniscr %>%
  select(Reference, Year, TBNI_Score, matches('Score')) %>% 
  gather('var', 'val', -Reference, -Year) %>% 
  group_by(Reference, Year, var) %>% 
  summarise(val = mean(val, na.rm = T)) %>% 
  spread(var, val) %>% 
  ungroup %>% 
  mutate(
    Action = findInterval(TBNI_Score, perc),
    outcome = factor(Action, levels = c('0', '1', '2'), labels = c('red', 'yellow', 'green')),
    outcome = as.character(outcome),
    Action = factor(Action, levels = c('0', '1', '2'), labels = c('On Alert', 'Caution', 'Stay the Course'))
  )

# fimstations, clip by segment extents
tbnistations <- fimstations %>% 
  left_join(tbniscr, by = 'Reference') %>% 
  select(Reference, Year, Month, TBNI_Score) %>% 
  mutate(
    Action = findInterval(TBNI_Score, perc),
    outcome = factor(Action, levels = c('0', '1', '2'), labels = c('red', 'yellow', 'green')),
    outcome = as.character(outcome),
    Action = factor(Action, levels = c('0', '1', '2'), labels = c('On Alert', 'Caution', 'Stay the Course'))
  )

avedat <- anlz_tbniave(tbniscr) 
```

```{r reactives}
# site attainment map
attmap <- reactive({
  
  # inputs
  yrsel1 <- input$yrsel1

  # site scores
  toplo <- tbniyrs %>% 
    filter(Year %in% yrsel1) %>% 
    select(Reference, TBNI_Score, Action, outcome) %>% 
    right_join(fimstations, ., by = 'Reference')
  
  # segment averages
  attyr <- avedat %>% 
    filter(Year %in% yrsel1) %>% 
    left_join(tbseg, ., by = 'bay_segment') 

  # map with custom legends
  mapview(attyr, fill = F, homebutton = F, popup = NULL, legend = F) %>% 
    .@map %>% 
    clearMarkers() %>% 
    leafem::removeMouseCoordinates() %>%
    addPolygons(
      data = attyr, 
      stroke = T, 
      color = 'grey', 
      weight = 1, 
      layerId = ~long_name, 
      fillColor = ~outcome, 
      fillOpacity = 0.3,
      label = ~paste0(bay_segment, ': ', long_name, ', Action: ', Action, ' (', outcome, ')')
    ) %>% 
    addCircleMarkers(
      data = toplo, 
      layerId = ~Reference,
      stroke = TRUE,
      color = 'black',
      fill = TRUE,
      fillColor = ~outcome,
      weight = 1,
      fillOpacity = 1,
      radius= 4,#~scales::rescale(val, from = scls, to = c(5, 20)),
      label = ~paste0('Station ', Reference, ' (TBNI: ', round(TBNI_Score, 1), ', action: ', Action)
    ) %>% 
    addLegend("topright", labels = c("Stay the Course", "Caution", "On Alert"), colors = rev(cols), title = "Bay segment/site <br>matrix outcomes", opacity = 1)
  
})

# site attainment matrix
attmat <- reactive({
  
  # input
  yrsel1 <- input$yrsel1

  lwid <- 1.5
  
  # base matrix with selected year
  p <- tbnimat +
    geom_hline(yintercept = yrsel1 - 0.5, lwd = lwid) + 
    geom_hline(yintercept = yrsel1 + 0.5, lwd = lwid) + 
    geom_segment(aes(x = 0.5, xend = 0.5, y = yrsel1 - 0.5, yend = yrsel1 + 0.5, lwd = lwid)) +
    geom_segment(aes(x = 4.5, xend = 4.5, y = yrsel1 - 0.5, yend = yrsel1 + 0.5, lwd = lwid))
  
  out <- show_matrixplotly(p, tooltip = 'Action')
  
  return(out)

})

# time series plots
timplo <- reactive({
  
  # input
  yrsel1 <- input$yrsel1
  
  out <- show_tbniscr(tbniscr, plotly = T) %>% 
    add_segments(x = yrsel1, xend = yrsel1, y = 0, yend = 100, line = list(color = 'red'), name = yrsel1)
  
  return(out)
  
})

# select fim years on station selection plot
fimyrs <- reactive({
  
  # input
  yrsel2 <- input$yrsel2
  
  # filter by year to map
  out <- tbnistations %>% 
    filter(Year >= yrsel2[1] & Year <= yrsel2[2])
  
  return(out)
  
})

# select fim station data
fimplo <- reactive({
  
  req(selsit)
  
  out <- tbniscr %>% 
    filter(Reference %in% selsit)
 
  return(out)
  
})

# select fim station data text
fimtxt <- reactive({
  
  # input
  fimplo <- fimplo()
  
  out <- paste0('Station: ', fimplo$Reference, ', Year: ', fimplo$Year, ', Month: ', fimplo$Month)
 
  return(out)
  
})

# tbni gauge
tbnigauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$TBNI_Score, min = 0, max = 100, label = 'TBNI score', gaugeSectors(
     danger = c(0, perc[1]), warning = perc, success = c(perc[2], 100),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# number of taxa gauge
numtgauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$ScoreNumTaxa, min = 0, max = 10, label = 'Taxa score', gaugeSectors(
     danger = c(0, 3), warning = c(3, 7), success = c(7, 10),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# number of benthic taxa gauge
bentgauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$ScoreBenthicTaxa, min = 0, max = 10, label = 'Benthic score', gaugeSectors(
     danger = c(0, 3), warning = c(3, 7), success = c(7, 10),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# selected taxa gauge
seltgauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$ScoreTaxaSelect, min = 0, max = 10, label = 'Selected score', gaugeSectors(
     danger = c(0, 3), warning = c(3, 7), success = c(7, 10),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# number of guilds gauge
guilgauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$ScoreNumGuilds, min = 0, max = 10, label = 'Guilds score', gaugeSectors(
     danger = c(0, 3), warning = c(3, 7), success = c(7, 10),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# shannon diversity gauge
shangauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- gauge(fimplo$ScoreShannon, min = 0, max = 10, label = 'Shannon score', gaugeSectors(
     danger = c(0, 3), warning = c(3, 7), success = c(7, 10),
     colors = rev(cols)
    ))
  
  return(out)
  
})

# number of taxa gauge, raw
numtrgauge <- reactive({
  
  # input
  fimplo <- fimplo()
  
  out <- rawgaugefun(fimplo, tbniscr, cols, 'NumTaxa', 'Number of Taxa')
  
  return(out)
  
})

# number of benthic taxa gauge, raw
bentrgauge <- reactive({
  
  # input
  fimplo <- fimplo()
  
  out <- rawgaugefun(fimplo, tbniscr, cols, 'BenthicTaxa', 'Benthic Taxa')

  return(out)
  
})

# selected taxa gauge, raw
seltrgauge <- reactive({
  
  # input
  fimplo <- fimplo()

  out <- rawgaugefun(fimplo, tbniscr, cols, 'TaxaSelect', 'Selected Taxa')

  return(out)
  
})

# number of guilds gauge, raw
guilrgauge <- reactive({
  
  # input
  fimplo <- fimplo()
  
  out <- rawgaugefun(fimplo, tbniscr, cols, 'NumGuilds', 'Number of Guilds')
  
  return(out)
  
})

# shannon diversity gauge, raw
shanrgauge <- reactive({
  
  # input
  fimplo <- fimplo()
  
  out <- rawgaugefun(fimplo, tbniscr, cols, 'Shannon', 'Shannon diversity')
  
  return(out)
  
})
```

```{r map}
# site selection map
output$map <- renderLeaflet({
  
  # input
  fimyrs <- fimyrs()
  
  # map with custom legends
  mapview(fimyrs, fill = F, homebutton = F, popup = NULL, legend = F) %>% 
    .@map %>% 
    leaflet::clearMarkers() %>%
    addCircleMarkers(
      data = fimyrs, 
      layerId = ~Reference,
      stroke = F,
      fillOpacity = 0.5,
      radius = 7, 
      fillColor = ~outcome,
      label = ~paste0('Station ', Reference, ' (TBNI: ', round(TBNI_Score, 1), ', action: ', Action)
    )
  
})

# leaflet proxy for marker select
map <- leafletProxy('map')

# binding for marker select and year
makeReactiveBinding('selsit')

# the selection
observeEvent(input$map_marker_click, {
  selsit <<- input$map_marker_click$id
})

observeEvent(input$map_marker_click, {

  # inputs
  fimyrs <- fimyrs()
  
  # filter the pour points by selection
  selsitplo <- fimyrs %>%
    filter(Reference %in% selsit)

  # clear markers on input select, add all points and selected point
  map <- map %>%
    leaflet::clearMarkers() %>%
    addCircleMarkers(
      data = fimyrs,
      layerId = ~Reference,
      stroke = F,
      fillOpacity = 0.5,
      radius = 7, 
      fillColor = ~outcome,
      label = ~paste0('Station ', Reference, ' (TBNI: ', round(TBNI_Score, 1), ', action: ', Action)
    ) %>%    
    addCircleMarkers(
      data = selsitplo,
      layerId = ~Reference,
      stroke = F,
      color = '#5C4A42',
      fill = T,
      radius = 14,
      fillOpacity = 0.9, 
      label = ~Reference
    )

})
```

```{r downloadhandlers}

```

OVERVIEW
===========================================================

Column {.tabset .tabset-fade data-width=650}
-----------------------------------------------------------------------

### USING THE DASHBOARD

<div class = "row">
<div class = "col-md-2"></div>
<div class = "col-md-8">

#### WELCOME TO THE TAMPA BAY NEKTON INDEX DASHBOARD!

```{r, echo = F, out.width = '100%', fig.align = 'center'}
knitr::include_graphics('www/bannerimage.png')
```

The Tampa Bay Nekton Index (TBNI) is a multimetric assessment method that quantifies the ecological health of the nekton community in Tampa Bay. The index provides a complementary approach to evaluating environmental condition that is supported by other assessment methods currently available for Tampa Bay (e.g., water quality report card, Benthic index, etc.).

</div>
<div class = "col-md-2"></div>
</div>

### METHODS

### DOWNLOAD DATA

1. BAY SEGMENT RESULTS
===========================================================

Column {.tabset .tabset-fade data-width=275}
-----------------------------------------------------------------------

### MATRIX RESULTS

```{r}
output$attmat <- renderPlotly(attmat())
plotlyOutput('attmat')
```

### TIME SERIES PLOTS

```{r}
output$timplo <- renderPlotly(timplo())
plotlyOutput('timplo')
```


### Using this tab

Column {data-width=500}
-----------------------------------------------------------------------

### RESULTS BY YEAR

```{r}
output$attmap <- renderLeaflet({attmap()})
fillCol(flex = c(NA, 1),
  column(12,
    column(6,
      sliderInput('yrsel1', 'Select year:', min = 1998, max = maxyr, value = maxyr, step = 1, sep = '', width = '200%', animate = T),
    )
  ),
  leafletOutput('attmap')
)
```

2 SITE TRENDS
===========================================================

Column {.tabset .tabset-fade data-width=275}
-----------------------------------------------------------------------

### MAP SELECTION

```{r}
leafletOutput('map')
```

### Using this tab

Column {data-width=500}
-----------------------------------------------------------------------

### PLOT

```{r}
output$tbnigauge <- renderGauge(tbnigauge())
output$numtgauge <- renderGauge(numtgauge())
output$bentgauge <- renderGauge(bentgauge())
output$seltgauge <- renderGauge(seltgauge())
output$guilgauge <- renderGauge(guilgauge())
output$shangauge <- renderGauge(shangauge())
output$numtrgauge <- renderGauge(numtrgauge())
output$bentrgauge <- renderGauge(bentrgauge())
output$seltrgauge <- renderGauge(seltrgauge())
output$guilrgauge <- renderGauge(guilrgauge())
output$shanrgauge <- renderGauge(shanrgauge())

fillCol(flex = c(NA, NA, 1, NA, 1, NA, 1),
  column(12,
    column(6,
      sliderInput('yrsel2', 'Select year range on map:', min = 1998, max = maxyr, value = c(1998, maxyr), step = 1, sep = '', width = '200%')
    ), 
    column(6, renderUI(fimtxt()))
  ),
  h4('TBNI total'), 
  fillRow(
    gaugeOutput('tbnigauge')
    ), 
  h4('Scored metrics'), 
  fillRow(
    gaugeOutput('numtgauge'),
    gaugeOutput('bentgauge'),
    gaugeOutput('seltgauge'),
    gaugeOutput('guilgauge'),
    gaugeOutput('shangauge')
    ),
  h4('Raw metrics'), 
  fillRow(
    gaugeOutput('numtrgauge'),
    gaugeOutput('bentrgauge'),
    gaugeOutput('seltrgauge'),
    gaugeOutput('guilrgauge'),
    gaugeOutput('shanrgauge')
    )
)
```

